/* Local runtime config — no remote source (assets are served locally).
   On Netlify this file is replaced at deploy time with:
     window.GALLERY_CONFIG = { sourceBase: 'https://<tunnel>.ngrok-free.dev' };
   You can also override at runtime with ?src=https://<tunnel> in the URL. */
window.GALLERY_CONFIG = {};
